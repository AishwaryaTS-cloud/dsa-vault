{
  "name": "dsa-pattern-vault",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack MERN application to track DSA questions and sync solutions with GitHub",
  "scripts": {
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev"
  }
}
